# dependencies
/node_modules

# next.js
/.next/
/out/

# rust / anchor build output
/target
/.anchor

# local Solana identities and program keypairs — never commit these
*-keypair.json

# env files. Every variable is public, but a local .env still points at a
# specific deployment and RPC key.
.env
.env.local

# typescript
*.tsbuildinfo
next-env.d.ts

# tooling and editors
.DS_Store
*.pem
.vercel
/coverage
npm-debug.log*
yarn-debug.log*
yarn-error.log*
