import { ProfileEditorRoute } from "@/components/profile-editor-route";

export const metadata = { title: "Your page" };

/** The same editor as `/claim`, under the URL a returning creator looks for. */
export default ProfileEditorRoute;
