/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findProfilePda, findUsernameRecordPda } from "../pdas";
import { TIPMARK_PROTOCOL_PROGRAM_ADDRESS } from "../programs";

export const CREATE_PROFILE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  225, 205, 234, 143, 17, 186, 50, 220,
]);

export function getCreateProfileDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_PROFILE_DISCRIMINATOR,
  );
}

export type CreateProfileInstruction<
  TProgram extends string = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountPayoutWallet extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountUsernameRecord extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountPayoutWallet extends string
        ? ReadonlyAccount<TAccountPayoutWallet>
        : TAccountPayoutWallet,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountProfile extends string
        ? WritableAccount<TAccountProfile>
        : TAccountProfile,
      TAccountUsernameRecord extends string
        ? WritableAccount<TAccountUsernameRecord>
        : TAccountUsernameRecord,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateProfileInstructionData = {
  discriminator: ReadonlyUint8Array;
  username: string;
  metadataUri: string;
  metadataHash: ReadonlyUint8Array;
};

export type CreateProfileInstructionDataArgs = {
  username: string;
  metadataUri: string;
  metadataHash: ReadonlyUint8Array;
};

export function getCreateProfileInstructionDataEncoder(): Encoder<CreateProfileInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["username", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["metadataUri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["metadataHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: CREATE_PROFILE_DISCRIMINATOR }),
  );
}

export function getCreateProfileInstructionDataDecoder(): Decoder<CreateProfileInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["username", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["metadataUri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["metadataHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getCreateProfileInstructionDataCodec(): Codec<
  CreateProfileInstructionDataArgs,
  CreateProfileInstructionData
> {
  return combineCodec(
    getCreateProfileInstructionDataEncoder(),
    getCreateProfileInstructionDataDecoder(),
  );
}

export type CreateProfileAsyncInput<
  TAccountOwner extends string = string,
  TAccountPayoutWallet extends string = string,
  TAccountConfig extends string = string,
  TAccountProfile extends string = string,
  TAccountUsernameRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  payoutWallet: Address<TAccountPayoutWallet>;
  config?: Address<TAccountConfig>;
  profile?: Address<TAccountProfile>;
  usernameRecord?: Address<TAccountUsernameRecord>;
  systemProgram?: Address<TAccountSystemProgram>;
  username: CreateProfileInstructionDataArgs["username"];
  metadataUri: CreateProfileInstructionDataArgs["metadataUri"];
  metadataHash: CreateProfileInstructionDataArgs["metadataHash"];
};

export async function getCreateProfileInstructionAsync<
  TAccountOwner extends string,
  TAccountPayoutWallet extends string,
  TAccountConfig extends string,
  TAccountProfile extends string,
  TAccountUsernameRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: CreateProfileAsyncInput<
    TAccountOwner,
    TAccountPayoutWallet,
    TAccountConfig,
    TAccountProfile,
    TAccountUsernameRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateProfileInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountPayoutWallet,
    TAccountConfig,
    TAccountProfile,
    TAccountUsernameRecord,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TIPMARK_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    payoutWallet: { value: input.payoutWallet ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    profile: { value: input.profile ?? null, isWritable: true },
    usernameRecord: { value: input.usernameRecord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }
  if (!accounts.profile.value) {
    accounts.profile.value = await findProfilePda(
      {
        owner: getAddressFromResolvedInstructionAccount(
          "owner",
          accounts.owner.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.usernameRecord.value) {
    accounts.usernameRecord.value = await findUsernameRecordPda(
      {
        username: getNonNullResolvedInstructionInput("username", args.username),
      },
      { programAddress },
    );
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("payoutWallet", accounts.payoutWallet),
      getAccountMeta("config", accounts.config),
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("usernameRecord", accounts.usernameRecord),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateProfileInstructionDataEncoder().encode(
      args as CreateProfileInstructionDataArgs,
    ),
    programAddress,
  } as CreateProfileInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountPayoutWallet,
    TAccountConfig,
    TAccountProfile,
    TAccountUsernameRecord,
    TAccountSystemProgram
  >);
}

export type CreateProfileInput<
  TAccountOwner extends string = string,
  TAccountPayoutWallet extends string = string,
  TAccountConfig extends string = string,
  TAccountProfile extends string = string,
  TAccountUsernameRecord extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  payoutWallet: Address<TAccountPayoutWallet>;
  config: Address<TAccountConfig>;
  profile: Address<TAccountProfile>;
  usernameRecord: Address<TAccountUsernameRecord>;
  systemProgram?: Address<TAccountSystemProgram>;
  username: CreateProfileInstructionDataArgs["username"];
  metadataUri: CreateProfileInstructionDataArgs["metadataUri"];
  metadataHash: CreateProfileInstructionDataArgs["metadataHash"];
};

export function getCreateProfileInstruction<
  TAccountOwner extends string,
  TAccountPayoutWallet extends string,
  TAccountConfig extends string,
  TAccountProfile extends string,
  TAccountUsernameRecord extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: CreateProfileInput<
    TAccountOwner,
    TAccountPayoutWallet,
    TAccountConfig,
    TAccountProfile,
    TAccountUsernameRecord,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateProfileInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountPayoutWallet,
  TAccountConfig,
  TAccountProfile,
  TAccountUsernameRecord,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TIPMARK_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    payoutWallet: { value: input.payoutWallet ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    profile: { value: input.profile ?? null, isWritable: true },
    usernameRecord: { value: input.usernameRecord ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("payoutWallet", accounts.payoutWallet),
      getAccountMeta("config", accounts.config),
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("usernameRecord", accounts.usernameRecord),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateProfileInstructionDataEncoder().encode(
      args as CreateProfileInstructionDataArgs,
    ),
    programAddress,
  } as CreateProfileInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountPayoutWallet,
    TAccountConfig,
    TAccountProfile,
    TAccountUsernameRecord,
    TAccountSystemProgram
  >);
}

export type ParsedCreateProfileInstruction<
  TProgram extends string = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    payoutWallet: TAccountMetas[1];
    config: TAccountMetas[2];
    profile: TAccountMetas[3];
    usernameRecord: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CreateProfileInstructionData;
};

export function parseCreateProfileInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateProfileInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      payoutWallet: getNextAccount(),
      config: getNextAccount(),
      profile: getNextAccount(),
      usernameRecord: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateProfileInstructionDataDecoder().decode(instruction.data),
  };
}
