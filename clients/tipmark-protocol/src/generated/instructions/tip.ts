/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { TIPMARK_PROTOCOL_PROGRAM_ADDRESS } from "../programs";

export const TIP_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  77, 164, 35, 21, 36, 121, 213, 51,
]);

export function getTipDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(TIP_DISCRIMINATOR);
}

export type TipInstruction<
  TProgram extends string = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
  TAccountSupporter extends string | AccountMeta<string> = string,
  TAccountProfile extends string | AccountMeta<string> = string,
  TAccountPayoutWallet extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSupporter extends string
        ? WritableSignerAccount<TAccountSupporter> &
            AccountSignerMeta<TAccountSupporter>
        : TAccountSupporter,
      TAccountProfile extends string
        ? ReadonlyAccount<TAccountProfile>
        : TAccountProfile,
      TAccountPayoutWallet extends string
        ? WritableAccount<TAccountPayoutWallet>
        : TAccountPayoutWallet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type TipInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  reference: ReadonlyUint8Array;
};

export type TipInstructionDataArgs = {
  amount: number | bigint;
  reference: ReadonlyUint8Array;
};

export function getTipInstructionDataEncoder(): FixedSizeEncoder<TipInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
      ["reference", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: TIP_DISCRIMINATOR }),
  );
}

export function getTipInstructionDataDecoder(): FixedSizeDecoder<TipInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
    ["reference", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getTipInstructionDataCodec(): FixedSizeCodec<
  TipInstructionDataArgs,
  TipInstructionData
> {
  return combineCodec(
    getTipInstructionDataEncoder(),
    getTipInstructionDataDecoder(),
  );
}

export type TipInput<
  TAccountSupporter extends string = string,
  TAccountProfile extends string = string,
  TAccountPayoutWallet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  supporter: TransactionSigner<TAccountSupporter>;
  profile: Address<TAccountProfile>;
  payoutWallet: Address<TAccountPayoutWallet>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: TipInstructionDataArgs["amount"];
  reference: TipInstructionDataArgs["reference"];
};

export function getTipInstruction<
  TAccountSupporter extends string,
  TAccountProfile extends string,
  TAccountPayoutWallet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: TipInput<
    TAccountSupporter,
    TAccountProfile,
    TAccountPayoutWallet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): TipInstruction<
  TProgramAddress,
  TAccountSupporter,
  TAccountProfile,
  TAccountPayoutWallet,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TIPMARK_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    supporter: { value: input.supporter ?? null, isWritable: true },
    profile: { value: input.profile ?? null, isWritable: false },
    payoutWallet: { value: input.payoutWallet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("supporter", accounts.supporter),
      getAccountMeta("profile", accounts.profile),
      getAccountMeta("payoutWallet", accounts.payoutWallet),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getTipInstructionDataEncoder().encode(args as TipInstructionDataArgs),
    programAddress,
  } as TipInstruction<
    TProgramAddress,
    TAccountSupporter,
    TAccountProfile,
    TAccountPayoutWallet,
    TAccountSystemProgram
  >);
}

export type ParsedTipInstruction<
  TProgram extends string = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    supporter: TAccountMetas[0];
    profile: TAccountMetas[1];
    payoutWallet: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: TipInstructionData;
};

export function parseTipInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedTipInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      supporter: getNextAccount(),
      profile: getNextAccount(),
      payoutWallet: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getTipInstructionDataDecoder().decode(instruction.data),
  };
}
