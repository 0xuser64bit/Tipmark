/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { TIPMARK_PROTOCOL_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_PROTOCOL_AUTHORITY_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([196, 230, 103, 192, 225, 211, 253, 246]);

export function getProposeProtocolAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROPOSE_PROTOCOL_AUTHORITY_DISCRIMINATOR,
  );
}

export type ProposeProtocolAuthorityInstruction<
  TProgram extends string = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      ...TRemainingAccounts,
    ]
  >;

export type ProposeProtocolAuthorityInstructionData = {
  discriminator: ReadonlyUint8Array;
  pendingAuthority: Address;
};

export type ProposeProtocolAuthorityInstructionDataArgs = {
  pendingAuthority: Address;
};

export function getProposeProtocolAuthorityInstructionDataEncoder(): FixedSizeEncoder<ProposeProtocolAuthorityInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["pendingAuthority", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PROPOSE_PROTOCOL_AUTHORITY_DISCRIMINATOR,
    }),
  );
}

export function getProposeProtocolAuthorityInstructionDataDecoder(): FixedSizeDecoder<ProposeProtocolAuthorityInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["pendingAuthority", getAddressDecoder()],
  ]);
}

export function getProposeProtocolAuthorityInstructionDataCodec(): FixedSizeCodec<
  ProposeProtocolAuthorityInstructionDataArgs,
  ProposeProtocolAuthorityInstructionData
> {
  return combineCodec(
    getProposeProtocolAuthorityInstructionDataEncoder(),
    getProposeProtocolAuthorityInstructionDataDecoder(),
  );
}

export type ProposeProtocolAuthorityAsyncInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config?: Address<TAccountConfig>;
  pendingAuthority: ProposeProtocolAuthorityInstructionDataArgs["pendingAuthority"];
};

export async function getProposeProtocolAuthorityInstructionAsync<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: ProposeProtocolAuthorityAsyncInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProposeProtocolAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TIPMARK_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
    ],
    data: getProposeProtocolAuthorityInstructionDataEncoder().encode(
      args as ProposeProtocolAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as ProposeProtocolAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type ProposeProtocolAuthorityInput<
  TAccountAuthority extends string = string,
  TAccountConfig extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  config: Address<TAccountConfig>;
  pendingAuthority: ProposeProtocolAuthorityInstructionDataArgs["pendingAuthority"];
};

export function getProposeProtocolAuthorityInstruction<
  TAccountAuthority extends string,
  TAccountConfig extends string,
  TProgramAddress extends Address = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
>(
  input: ProposeProtocolAuthorityInput<TAccountAuthority, TAccountConfig>,
  config?: { programAddress?: TProgramAddress },
): ProposeProtocolAuthorityInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountConfig
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? TIPMARK_PROTOCOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("config", accounts.config),
    ],
    data: getProposeProtocolAuthorityInstructionDataEncoder().encode(
      args as ProposeProtocolAuthorityInstructionDataArgs,
    ),
    programAddress,
  } as ProposeProtocolAuthorityInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountConfig
  >);
}

export type ParsedProposeProtocolAuthorityInstruction<
  TProgram extends string = typeof TIPMARK_PROTOCOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    config: TAccountMetas[1];
  };
  data: ProposeProtocolAuthorityInstructionData;
};

export function parseProposeProtocolAuthorityInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeProtocolAuthorityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { authority: getNextAccount(), config: getNextAccount() },
    data: getProposeProtocolAuthorityInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
